import { FlexBox } from "components/containers";
import { Label } from "components/generic";

export const HostChats: React.FC = () => {
  return (
    <FlexBox>
      <Label>Page Host Chats</Label>
    </FlexBox>
  );
};