import { FlexBox } from "components/containers";
import { Label } from "components/generic";

export const HostRooms: React.FC = () => {
  return (
    <FlexBox>
      <Label>Page Hosts Rooms</Label>
    </FlexBox>
  );
};