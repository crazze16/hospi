import { FlexBox } from "components/containers";
import { Label } from "components/generic";

export const HostMatches: React.FC = () => {
  return (
    <FlexBox>
      <Label>Page Host Matches</Label>
    </FlexBox>
  );
};