import { ContainerView } from "components/containers";
import { MenuLeftSide } from "components/MenuLeftSide";

export const StudentRooms: React.FC = () => {
  return (
    <ContainerView>
      <MenuLeftSide />
      <span></span>
    </ContainerView>
  );
};
