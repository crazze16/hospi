import { FlexBox } from "components/containers";
import { Label } from "components/generic";

export const BecomeHost: React.FC = () => {
  return (
    <FlexBox>
      <Label>Page Become Host</Label>
    </FlexBox>
  );
};
