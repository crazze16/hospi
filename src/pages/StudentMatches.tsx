import { FlexBox } from "components/containers";
import { Label } from "components/generic";

export const StudentMatches: React.FC = () => {
  return (
    <FlexBox>
      <Label>Page Matches Student requests</Label>
    </FlexBox>
  );
};