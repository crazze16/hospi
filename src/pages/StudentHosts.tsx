import { FlexBox } from "components/containers";
import { Label } from "components/generic";

export const StudentHosts: React.FC = () => {
  return (
    <FlexBox>
      <Label>Page Host for student</Label>
    </FlexBox>
  );
};